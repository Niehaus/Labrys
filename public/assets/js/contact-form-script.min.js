$("#contactForm").validator().on("submit", function (event) {
    if (event.isDefaultPrevented()) {
        // handle the invalid form...
        formError();
        submitMSG(false, "Certifique-se de ter preenchido os dados corretamente!");
    } else {
        // everything looks good!
        submitMSG(true, "Denuncia Recebida!");
        event.preventDefault();
        submitForm();
    }

});

function setPopUp() {
  var checkrsFis = document.getElementsByName("agr-fisica");
  var checkrsSimbol = document.getElementsByName("agr-simbol");
  popupText = "\nOcorreu\n";

  if (checkrsFis[0].checked) {
    popupText+= checkrsFis[0].value;
  }
  if (checkrsFis[1].checked) {
    popupText+= "\n";
    popupText+= checkrsFis[1].value;
  }
  if (checkrsSimbol[0].checked) {
    popupText+= "\n";
    popupText+= checkrsSimbol[0].value;
  }
  if (checkrsSimbol[1].checked) {
    popupText+= "\n";
    popupText+= checkrsSimbol[1].value;
  }
  if (checkrsSimbol[2].checked) {
    popupText+= "\n";
    popupText+= checkrsSimbol[2].value;
  }
return popupText;
}

function submitForm(){
    // Initiate Variables With Form Content
    var endereco = $("#name").val();
    var local = $("#email").val();
    var msg_subject = $("#msg_subject").val();
    var message = $("#message").val();

}

function formSuccess(){
    $("#contactForm")[0].reset();
    submitMSG(true, "Message Submitted!");
}

function formError(){
    $("#contactForm").removeClass().addClass('shake animated').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', function(){
        $(this).removeClass();
    });
}

function submitMSG(valid, msg){
    if(valid){
        var msgClasses = "h3 text-left tada animated text-success";
    } else {
        var msgClasses = "h3 text-left text-danger";
    }
    $("#msgSubmit").removeClass().addClass(msgClasses).text(msg);
}
